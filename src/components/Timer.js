function Timer() {
  // Add logic to change label
  return (
    <div id="timer-div" className="d-flex flex-column align-items-center">
      <label id="timer-label">Focus</label>
      <div id="time-left">00:00</div>
    </div>
  );
}

export default Timer;
